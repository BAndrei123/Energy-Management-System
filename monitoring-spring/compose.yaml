

services:
  db:
    image: postgres:14
    restart: always
    environment:
      POSTGRES_DB: 'monitoring-db'
      POSTGRES_USER: 'root'
      POSTGRES_PASSWORD: 'root'
    ports:
      - '5434:5432'
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: